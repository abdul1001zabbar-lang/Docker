apiVersion: batch/v1
kind: Job
metadata:
  name: job-demo
spec:
  template:
    spec:
      containers:
      - name: job
        image: busybox
        command: ["echo", "Hello Job"]
      restartPolicy: Never
``